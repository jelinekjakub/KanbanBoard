import { SVGAttributes } from 'react';

export default function ApplicationLogo(props: SVGAttributes<SVGElement>) {
    return (
        <span className='tracking-tighter text-lg'>kBoard</span>
    );
}
